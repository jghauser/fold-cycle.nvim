local function is_fold_closed(line)
  local folded
  if fn.foldclosed(line) == -1 then
    folded = false
  else
    folded = true
  end
  return folded
end

local function is_fold_closed(line)
  local folded
  if fn.foldclosed(line) == -1 then
    folded = false
  else
    folded = true
  end
  return folded
end
